const {createNewPostValidator} = require('../post/newPostValidator');
const {updatePostValidator} = require('../post/updatePostValidator');

module.exports = {
    createNewPostValidator,
    updatePostValidator
}